---
title: "Untitled"
output: html_document
date: "2022-10-20"
---

Blank Rmd for debugging and troubleshooting

```{r}

t.dataset_completeness %>% count(dataset) %>% arrange(n)
t.dataset_dashboard %>% arrange(Dataset)

strSys.Date()
str_remove_all(Sys.Date(),"-")


archived_on = 
        t.dataset_overview %>%
  left_join(datasets_available%>%select(dataset=Dataset,exported)) %>%
          filter(dataset == "gdppr") %>%
          select(archived_on,exported) %>% 
          pivot_longer(everything()) %>% 
          pull(value)
      
        
nation_summary = "England"
export_date_england = "2022-10-02"
export_date_scotland = Sys.Date() + 1


paste0(paste(paste0("<b>",c("Archived On","Exported On"),":</b>"),
                                    c(if(length(archived_on==0)){"s"} else {archived_on},
                                      if(nation_summary=="England"){export_date_england} else if (nation_summary=="Scotland"){export_date_scotland} else {export_date_wales}),
                                    collapse = '<br/>'),"<br/>&nbsp")

length(archived_on)


max(export_date_england,export_date_scotland,export_date_wales)


dataset_overview() %>%
          left_join(datasets_available%>%select(dataset=Dataset,exported)) %>%
          select(archived_on,exported) %>% 
          pivot_longer(everything()) %>% 
          pull(value)


```


